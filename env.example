# Microsoft Azure SSO Configuration
# =================================
# To enable Microsoft SSO authentication, configure the following environment variables:
# 1. Create an Azure AD application in the Azure portal
# 2. Copy the values from your Azure AD app registration
# 3. Create a .env.local file in the root directory with these values

# Your Azure AD Application (client) ID
NEXT_PUBLIC_AZURE_CLIENT_ID=your-client-id-here

# Your Azure AD Directory (tenant) ID  
# Format: https://login.microsoftonline.com/{tenant-id}
# Use "common" for multi-tenant, or specific tenant ID for single-tenant
NEXT_PUBLIC_AZURE_AUTHORITY=https://login.microsoftonline.com/your-tenant-id

# Redirect URI (should match what's configured in Azure AD)
# For development, this is typically http://localhost:3000
# For production, use your actual domain
NEXT_PUBLIC_REDIRECT_URI=http://localhost:3000

# ============================================
# Azure AD App Registration Configuration:
# ============================================
# 1. Go to Azure Portal > Azure Active Directory > App registrations
# 2. Click "New registration"
# 3. Set the redirect URI to match NEXT_PUBLIC_REDIRECT_URI above
# 4. Under "API permissions", add Microsoft Graph permissions:
#    - User.Read (delegated)
#    - Profile (delegated) 
#    - Email (delegated)
#    - OpenId (delegated)
# 5. Copy the Application (client) ID to NEXT_PUBLIC_AZURE_CLIENT_ID
# 6. Copy the Directory (tenant) ID to NEXT_PUBLIC_AZURE_AUTHORITY

# Email OTP Configuration (Optional - for custom email service)
# =============================================================
# If you want to use a custom email service for OTP instead of mock:
# EMAIL_SERVICE_API_URL=https://your-email-service.com/api
# EMAIL_SERVICE_API_KEY=your-api-key 